import { SubtitleSegment } from './models/transcription_segment';
export declare class VideoTranscriber {
    googleCredentialsPath: string;
    enableAutomaticPunctuation: boolean;
    silentLength: number;
    constructor(googleCredentialsPath: string, enableAutomaticPunctuation: boolean, silentLength?: number);
    transcribe(videoUrl: string, jobId: string): Promise<SubtitleSegment[]>;
}
