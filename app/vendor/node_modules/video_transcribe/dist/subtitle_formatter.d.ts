import { SubtitleSegment } from "./models/transcription_segment";
export declare class SubtitleFormatter {
    static _pad_with_zeroes(number: number, length: number): string;
    static _convertSecondsToTimestamp(value: number, isSRT?: boolean): string;
    static format(transcriptions: SubtitleSegment[], isSRT?: boolean): string;
}
