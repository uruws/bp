export declare class TranscriptionSegment {
    index: number;
    start: number;
    duration: number;
    file: string;
    transcription: string;
    constructor(index: number, start: number, duration: number, file: string);
    get end(): number;
}
export declare class SubtitleSegment {
    start: number;
    end: number;
    transcription: string;
    constructor(start: number, end: number, transcription: string);
}
