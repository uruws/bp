import { TranscriptionSegment, SubtitleSegment } from "./models/transcription_segment";
export declare class SubtitleGenerator {
    generate(transcriptions: TranscriptionSegment[]): Promise<SubtitleSegment[]>;
}
