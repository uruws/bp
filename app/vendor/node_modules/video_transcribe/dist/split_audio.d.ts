/// <reference types="node" />
import * as fs from 'fs';
import { TranscriptionSegment } from './models/transcription_segment';
export declare class SplitAudio {
    silentLength: number;
    constructor(silentLength: number);
    onSilence(sourceFilePath: string, jobId: string): Promise<TranscriptionSegment[]>;
    _generateTimeIndexes(audioFiles: fs.PathLike[]): Promise<unknown>;
    _zeroPad(num: number): string | number;
    _runFFmpeg(args: string[]): Promise<string>;
    _identifyPauses(sourceFilePath: string): Promise<{
        start: number;
        duration: number;
        end: number;
        middle: number;
    }[]>;
    _divideLongClips(clips: any): {
        start: number;
        duration: number;
    }[];
    _getClips(pauses: any, totalClipDuration: any): any[];
    _getListOfSplitFiles(directory: fs.PathLike): Promise<string[]>;
}
