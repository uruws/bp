import { v1p1beta1 } from '@google-cloud/speech';
import { Storage } from '@google-cloud/storage';
import { SubtitleSegment } from './models/transcription_segment';
export declare class SpeechToText {
    language: string;
    client: v1p1beta1.SpeechClient;
    enableAutomaticPunctuation: boolean;
    storage: Storage;
    constructor(credentialsPath: string, language: string, enableAutomaticPunctuation: boolean);
    _getAudioFromUrl(sourceFileurl: string, jobId: String): Promise<string>;
    _convertAudio(sourceFilePath: string, jobId: String): Promise<string>;
    _uploadToGoogleCloudStorage(wavPath: string, jobId: string): Promise<string>;
    _removeFromGoogleCloudStorage(jobId: string): void;
    generateSubs(audioUrl: string, jobId: string): Promise<SubtitleSegment[]>;
}
