import { v1p1beta1 } from '@google-cloud/speech';
import { TranscriptionSegment } from './models/transcription_segment';
export declare class AudioTrascriber {
    client: v1p1beta1.SpeechClient;
    enableAutomaticPunctuation: boolean;
    constructor(credentialsPath: string, enableAutomaticPunctuation: boolean);
    _transcribe(audioPath: string): Promise<string>;
    transcribeSegments(segments: TranscriptionSegment[]): Promise<TranscriptionSegment[]>;
}
