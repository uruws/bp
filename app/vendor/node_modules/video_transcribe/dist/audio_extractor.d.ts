export declare class AudioExtractor {
    _getVideoFromUrl(sourceFileurl: string, jobId: String): Promise<string>;
    _extractAudio(sourceFilePath: string, jobId: String): Promise<string>;
    extractFromUrl(url: string, jobId: string): Promise<string>;
    extractFromLocalFile(path: string, jobId: string): Promise<string>;
}
